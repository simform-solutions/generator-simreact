import React, { Component } from 'react'
import './<%= name %>.scss'

class <%= name %> extends Component {
  render() {
    return (
      <div><%= name %> works!</div>
    )
  }
}

export default <%= name %>
