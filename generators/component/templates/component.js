import React, { Component } from 'react'
import './<%= name %>.css'

class <%= name %> extends Component {
  state = {}
  render() {
    return (
      <div><%= name %> works!</div>
    )
  }
}

export default <%= name %>
